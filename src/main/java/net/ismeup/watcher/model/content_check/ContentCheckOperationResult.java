package net.ismeup.watcher.model.content_check;

import net.ismeup.watcher.interfaces.CheckOperationResult;

public class ContentCheckOperationResult extends CheckOperationResult {
    public ContentCheckOperationResult(boolean status) {
        super(status);
    }
}
